import React,{useEffect,useMemo,useState}from'react';import{createRoot}from'react-dom/client';import{Clock,Download,Pause,Play,Shuffle,Upload,Volume2}from'lucide-react';import Papa from'papaparse';import{motion,AnimatePresence}from'framer-motion';import'./styles/app.css';import{supabase}from'./lib/supabase';import{snakeTeamAt,roundFor,shuffle}from'./lib/draftLogic';import{exportDraft}from'./lib/exportExcel';
const logo='/revealdraft-logo.jpeg';
function App(){const[session,setSession]=useState(null);const[profile,setProfile]=useState(null);const[drafts,setDrafts]=useState([]);const[draftId,setDraftId]=useState(localStorage.draftId||'');useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session));return supabase.auth.onAuthStateChange((_,s)=>setSession(s)).data.subscription.unsubscribe},[]);useEffect(()=>{if(session)supabase.from('profiles').select('*').eq('id',session.user.id).single().then(({data})=>setProfile(data));},[session]);if(location.pathname.startsWith('/tv'))return <TV/>;if(!session)return <Login/>;return <Shell profile={profile} drafts={drafts} setDrafts={setDrafts} draftId={draftId} setDraftId={setDraftId}/>}
function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState('');async function login(){setErr('');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setErr(error.message)}return <div className="login"><img src={logo}/><h1>RevealDraft</h1><p>The live draft experience for rec sports.</p><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button onClick={login}>Sign In</button>{err&&<b className="bad">{err}</b>}</div>}
function Shell({profile,draftId,setDraftId}){const[drafts,setDrafts]=useState([]);const[data,setData]=useState({draft:null,teams:[],players:[],picks:[],queues:[]});useEffect(()=>{supabase.from('drafts').select('*').order('created_at',{ascending:false}).then(({data})=>{setDrafts(data||[]);if(!draftId&&data?.[0]){setDraftId(data[0].id);localStorage.draftId=data[0].id}})},[]);useEffect(()=>{if(!draftId)return;load();const ch=supabase.channel('draft-'+draftId).on('postgres_changes',{event:'*',schema:'public'},load).subscribe();return()=>supabase.removeChannel(ch)},[draftId]);async function load(){const[d,t,p,pk,q]=await Promise.all([supabase.from('drafts').select('*').eq('id',draftId).single(),supabase.from('teams').select('*').eq('draft_id',draftId),supabase.from('players').select('*').eq('draft_id',draftId),supabase.from('picks').select('*').eq('draft_id',draftId).order('pick_number'),supabase.from('queues').select('*').eq('draft_id',draftId).order('rank')]);setData({draft:d.data,teams:t.data||[],players:p.data||[],picks:pk.data||[],queues:q.data||[]})}
if(!profile)return <div className="loading">Loading…</div>;const isAdmin=profile.role==='admin',isComm=profile.role==='commissioner'||isAdmin;return <><header><div className="brand"><img src={logo}/><span>RevealDraft</span></div><select value={draftId} onChange={e=>{setDraftId(e.target.value);localStorage.draftId=e.target.value}}>{drafts.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><span className="pill">{profile.role}</span><button onClick={()=>supabase.auth.signOut()}>Sign Out</button></header><main>{isAdmin&&<Admin drafts={drafts} reload={()=>location.reload()}/>} {data.draft&&<DraftRoom data={data} profile={profile} isComm={isComm} reload={load}/>}</main></>}
function Admin({drafts,reload}){const[name,setName]=useState('');async function create(){if(!name)return;await supabase.from('drafts').insert({name,type:'coed'});reload()}return <section className="panel admin"><h2>Admin</h2><input placeholder="New draft name" value={name} onChange={e=>setName(e.target.value)}/><button onClick={create}>Create Draft</button><p className="muted">Create users in Supabase Authentication, then set their role in the profiles table.</p></section>}
function DraftRoom({data,profile,isComm,reload}){const{draft,teams,players,picks,queues}=data;const myTeam=teams.find(t=>t.coach_user_id===profile.id);const currentTeam=snakeTeamAt(teams,draft.current_pick_index);const currentPhase=draft.type==='coed'?draft.current_phase:'any';const available=players.filter(p=>!p.drafted_team_id&&!p.is_coach&&(currentPhase==='any'||p.gender===currentPhase||p.gender==='any'));const canPick=(isComm||currentTeam?.id===myTeam?.id)&&draft.status==='live';const[tab,setTab]=useState('available');const[flash,setFlash]=useState(null);useEffect(()=>{if(draft.reveal_pick_id){setFlash('pick');setTimeout(()=>setFlash('reveal'),2200);setTimeout(()=>setFlash(null),10000)}},[draft.reveal_pick_id]);async function makePick(player,team=currentTeam){if(!team||!player)return;const pickNo=picks.length+1;const phase=currentPhase;const{data:pick}=await supabase.from('picks').insert({draft_id:draft.id,pick_number:pickNo,phase,round_number:roundFor(teams,draft.current_pick_index),team_id:team.id,player_id:player.id,made_by:profile.id}).select().single();await supabase.from('players').update({drafted_team_id:team.id,drafted_pick_id:pick.id}).eq('id',player.id);await supabase.from('drafts').update(nextDraftState(draft,teams,players,pick.id)).eq('id',draft.id);beep();}
function nextDraftState(){let idx=draft.current_pick_index+1,phase=draft.current_phase;const remaining=players.filter(p=>!p.drafted_team_id&&!p.is_coach&&p.id!==arguments[3]);const phaseLeft=remaining.some(p=>p.gender===phase||p.gender==='any');if(draft.type==='coed'&&!phaseLeft){const next=draft.phase_order.find(x=>x!==phase);phase=next||phase;}return{current_pick_index:idx,current_phase:phase,reveal_pick_id:arguments[3],reveal_until:new Date(Date.now()+10000).toISOString(),pick_started_at:new Date().toISOString(),status:remaining.length?'live':'complete'}}
async function pause(status){await supabase.from('drafts').update({status,pick_started_at:new Date().toISOString()}).eq('id',draft.id)}
async function randomize(){const shuffled=shuffle(teams);for(let i=0;i<shuffled.length;i++)await supabase.from('teams').update({draft_order:i+1}).eq('id',shuffled[i].id);let nums=shuffle(players.map((_,i)=>i+1));for(let i=0;i<players.length;i++)await supabase.from('players').update({random_number:nums[i]}).eq('id',players[i].id);reload()}
async function addQueue(p){if(!myTeam)return;await supabase.from('queues').insert({draft_id:draft.id,team_id:myTeam.id,player_id:p.id,rank:(queues.filter(q=>q.team_id===myTeam.id).length+1)})}
return <section className="draft"><AnimatePresence>{flash&&<RevealOverlay mode={flash} pick={picks.find(p=>p.id===draft.reveal_pick_id)} players={players} teams={teams}/>}</AnimatePresence><div className="scorebar"><h1>{draft.name}</h1><div><b>On the clock:</b> {currentTeam?.name||'—'}</div><Timer draft={draft}/><div className="phase">Phase: {currentPhase}</div>{isComm&&<div className="actions"><button onClick={()=>pause('live')}><Play size={16}/>Start</button><button onClick={()=>pause('paused')}><Pause size={16}/>Pause</button><button onClick={randomize}><Shuffle size={16}/>Randomize</button><button onClick={()=>exportDraft(data)}><Download size={16}/>Export XLS</button></div>}</div><nav className="tabs">{['available','picks','roster','queue','teams','commissioner'].filter(x=>x!=='commissioner'||isComm).map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)}>{x}</button>)}</nav>{tab==='available'&&<PlayerGrid players={available} makePick={makePick} canPick={canPick} addQueue={addQueue}/>} {tab==='picks'&&<PickList picks={picks} players={players} teams={teams}/>} {tab==='roster'&&<Roster team={myTeam} players={players}/>} {tab==='queue'&&<Queue queues={queues} team={myTeam} players={players} makePick={makePick} canPick={canPick}/>} {tab==='teams'&&<AllRosters teams={teams} players={players}/>} {tab==='commissioner'&&<CommissionerTools draft={draft} teams={teams} players={players} makePick={makePick}/>}</section>}
function Timer({draft}){const[now,setNow]=useState(Date.now());useEffect(()=>{const i=setInterval(()=>setNow(Date.now()),250);return()=>clearInterval(i)},[]);let left=draft.pick_seconds;if(draft.pick_started_at&&draft.status==='live')left=Math.max(0,Math.ceil((new Date(draft.pick_started_at).getTime()+draft.pick_seconds*1000-now)/1000));return <div className={'timer '+(left<15?'danger':'')}><Clock size={20}/>{left}</div>}
function PlayerGrid({players,makePick,canPick,addQueue}){return <div className="grid">{players.sort((a,b)=>a.random_number-b.random_number).map(p=><div className="card"><h2>#{p.random_number}</h2><p>{p.primary_position} / {p.secondary_position}</p><small>{p.gender}</small><button disabled={!canPick} onClick={()=>makePick(p)}>Draft</button><button onClick={()=>addQueue(p)}>Queue</button></div>)}</div>}
function PickList({picks,players,teams}){return <table><tbody><tr><th>Pick</th><th>Team</th><th>Player</th><th>#</th><th>Pos</th></tr>{picks.map(pk=>{const p=players.find(x=>x.id===pk.player_id)||{},t=teams.find(x=>x.id===pk.team_id)||{};return <tr><td>{pk.pick_number}</td><td>{t.name}</td><td>{p.name}</td><td>{p.random_number}</td><td>{p.primary_position}/{p.secondary_position}</td></tr>})}</tbody></table>}
function Roster({team,players}){if(!team)return <p>No team assigned.</p>;return <div><h2>{team.name}</h2><PlayerNames players={players.filter(p=>p.drafted_team_id===team.id||p.assigned_team_id===team.id)}/></div>}
function AllRosters({teams,players}){return <div className="rosters">{teams.sort((a,b)=>a.draft_order-b.draft_order).map(t=><div className="panel"><h2>{t.logo_url&&<img className="teamlogo" src={t.logo_url}/>} {t.name}</h2><PlayerNames players={players.filter(p=>p.drafted_team_id===t.id||p.assigned_team_id===t.id)}/></div>)}</div>}
function PlayerNames({players}){return <ul>{players.map(p=><li><b>#{p.random_number}</b> {p.name} — {p.primary_position}/{p.secondary_position}</li>)}</ul>}
function Queue({queues,team,players,makePick,canPick}){const list=queues.filter(q=>q.team_id===team?.id).map(q=>players.find(p=>p.id===q.player_id)).filter(Boolean).filter(p=>!p.drafted_team_id);return <div className="grid">{list.map(p=><div className="card"><h2>#{p.random_number}</h2><p>{p.primary_position}/{p.secondary_position}</p><button disabled={!canPick} onClick={()=>makePick(p)}>Draft</button></div>)}</div>}
function CommissionerTools({draft,teams,players,makePick}){const[team,setTeam]=useState(''),[fileType,setFileType]=useState('players');async function importCsv(e){const file=e.target.files[0];Papa.parse(file,{header:true,complete:async r=>{if(fileType==='players')await supabase.from('players').insert(r.data.filter(x=>x.name).map((x,i)=>({draft_id:draft.id,random_number:+x.random_number||i+1,name:x.name,gender:(x.gender||'any').toLowerCase(),primary_position:x.primary_position,secondary_position:x.secondary_position})));else await supabase.from('teams').insert(r.data.filter(x=>x.name).map(x=>({draft_id:draft.id,name:x.name,logo_url:x.logo_url,draft_order:+x.draft_order||null})));location.reload()}})}return <div className="panel"><h2>Commissioner Tools</h2><select value={fileType} onChange={e=>setFileType(e.target.value)}><option value="players">Player CSV</option><option value="teams">Team CSV</option></select><label className="upload"><Upload/> Import CSV<input type="file" accept=".csv" onChange={importCsv}/></label><select value={team} onChange={e=>setTeam(e.target.value)}><option value="">Pick for team…</option>{teams.map(t=><option value={t.id}>{t.name}</option>)}</select><PlayerGrid players={players.filter(p=>!p.drafted_team_id&&!p.is_coach)} canPick={!!team} makePick={p=>makePick(p,teams.find(t=>t.id===team))} addQueue={()=>{}}/></div>}
function RevealOverlay({mode,pick,players,teams}){const p=players.find(x=>x.id===pick?.player_id)||{},t=teams.find(x=>x.id===pick?.team_id)||{};return <motion.div className="overlay" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}>{mode==='pick'?<motion.h1 initial={{scale:.6}} animate={{scale:1.05}}>THE PICK IS IN!</motion.h1>:<motion.div className="reveal" initial={{y:80}} animate={{y:0}}><h3>{t.name} selects</h3><h1>{p.name}</h1><h2>#{p.random_number}</h2><p>{p.primary_position} / {p.secondary_position}</p></motion.div>}</motion.div>}
function TV(){const[code,setCode]=useState(new URLSearchParams(location.search).get('code')||''),[data,setData]=useState(null);async function load(){const{data:d}=await supabase.from('drafts').select('*').eq('tv_code',code).single();if(d){const[teams,players,picks]=await Promise.all([supabase.from('teams').select('*').eq('draft_id',d.id),supabase.from('players').select('*').eq('draft_id',d.id),supabase.from('picks').select('*').eq('draft_id',d.id)]);setData({draft:d,teams:teams.data||[],players:players.data||[],picks:picks.data||[]})}}useEffect(()=>{if(code){load();const ch=supabase.channel('tv').on('postgres_changes',{event:'*',schema:'public'},load).subscribe();return()=>supabase.removeChannel(ch)}},[code]);if(!code)return <div className="login"><img src={logo}/><input placeholder="TV code" onChange={e=>setCode(e.target.value)}/></div>;if(!data)return <div className="loading">Loading TV mode…</div>;return <div className="tv"><RevealOverlay mode={data.draft.reveal_pick_id?'reveal':'pick'} pick={data.picks.find(p=>p.id===data.draft.reveal_pick_id)} players={data.players} teams={data.teams}/><Timer draft={data.draft}/></div>}
function beep(){try{new Audio('data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEAESsAACJWAAACABAAZGF0YQAAAAA=').play()}catch{}}
createRoot(document.getElementById('root')).render(<App/>);
